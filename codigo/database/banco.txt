CREATE DATABASE TTRAIO;

USE TTRAIO;

CREATE TABLE usuarios ( 
    id INT AUTO INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL, 
    password VARCHAR(100) NOT NULL 
);